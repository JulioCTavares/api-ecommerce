version: '3.8'
services:
  pg-dev:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=ecommerce
    volumes:
     - pgdata:/data/postgres
volumes:
  pgdata:
    driver: local